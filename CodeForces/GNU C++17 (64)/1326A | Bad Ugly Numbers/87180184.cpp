#include<string.h>
#include<stdio.h>
#include<stdlib.h>
#include <algorithm> 
#include <iostream> 
#include<vector>
#include<iomanip>
#include <regex>
 
using namespace std;
int main()
{
    long long n, num;
    cin >> n;
    for (int i = 0; i < n; i++)
    {
        cin >> num;
        if (num == 1)
            cout << -1;
        else
        {
            cout << 2;
            for (long long i = 1; i < num; i++)
            {
                cout << 7;
                
           }
        }
        cout << endl;
    }
}